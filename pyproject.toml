[project]
name = "atlas"
version = "0.1.0"
description = "Code quality and linting tools for Atlas workspace"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "pyrefly>=0.40.0",
    "ruff>=0.14.4",
]
