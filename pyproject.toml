[project]
name = "atlas"
version = "0.1.0"
description = "Argo Data Visualization Platform"
requires-python = ">=3.12"
dependencies = []

[tool.uv]
dev-dependencies = [
    "ruff>=0.1.0",
    "pyrefly>=0.1.0",
    "pytest>=7.4.0",
    "pytest-asyncio>=0.21.0",
]
